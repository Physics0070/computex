{
  "name": "computex",
  "version": "0.1.0",
  "private": true,
  "description": "ComputeX — pay-per-compute GPU marketplace settled in USDC on Algorand via x402",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "build": "npm run setup && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "check:payer": "npm --prefix server run check:payer",
    "test:flow": "npm --prefix server run test:flow"
  },
  "engines": {
    "node": ">=20"
  }
}
